import { PlayerPositionPipe } from './player-position.pipe';

describe('PlayerPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
